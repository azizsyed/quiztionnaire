// import { selectTestGeneratorPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectTestGeneratorPageDomain();

describe('selectTestGeneratorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
